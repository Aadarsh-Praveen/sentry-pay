# SentryPay — Phase 1 Dependencies
# Install with: pip install -r requirements.txt
#
# Python 3.14 + Apple Silicon (aarch64) compatible
# Using minimum version bounds instead of exact pins

# PDF extraction
pdfplumber>=0.10.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# scipy — only used for drift detection (Phase 4, not needed now)
# If this fails: pip install scipy --only-binary=:all: --pre
scipy>=1.11.0

# Synthetic data generation
Faker>=20.0.0

# Elastic
elasticsearch>=8.10.0

# Google Cloud
google-cloud-aiplatform>=1.50.0
google-cloud-bigquery>=3.20.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
feedparser>=6.0.0
tqdm>=4.65.0
colorama>=0.4.6

# Data generation & validation
openpyxl>=3.1.0